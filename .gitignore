# macOS
.DS_Store

# Editor / OS cruft
*~
.vscode/
Thumbs.db

# Local build/tmp (none needed for this static site)
node_modules/
dist/

# Secrets — never commit (depth tool dev-time Lemon Squeezy key lives elsewhere)
.env
.env.local
.env.*.local

# Polar sandbox config overrides (depth-map-generator). Local/dev only —
# production values live in the committed polar.config.js. See CLAUDE.md.
public/depth-map-generator/js/polar.config.local.js
